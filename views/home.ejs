<!DOCTYPE html>
<html lang="en">

<head>
    <title>Bingo</title>
    <style>
        table {
            background-color: #aa3333;
        }

        .numbers {
            background-color: azure;
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
            font-size: 30px;
            margin: 50px;
        }

        .bingo-headers {
            font-size: 50px;
            font-family: 'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
        }
    </style>
</head>

<body>
    Let's play bingo!
    <table style="border: 1px solid black; height: 600px; padding: 10px 20px; border-radius: 10px;">
        <thead style="margin-top: 50px;">
            <tr>
                <th class="bingo-headers" width="20%">B</th>
                <th class="bingo-headers" width="20%">I</th>
                <th class="bingo-headers" width="20%">N</th>
                <th class="bingo-headers" width="20%">G</th>
                <th class="bingo-headers" width="20%">O</th>
            </tr>
        </thead>
        <tbody>
            <% for(let i=5; i <=9; i++){ %>
                <tr style="text-align: center; ">
                    <% for(let j=0; j < 5; j++){ %>
                        <td class="numbers" width="100px" style="border: 1px solid black; border-radius: 10px;">
                            <%= bingocards[bingocards.length-1].cards[0].cardInfo[i].line[j] %>
                        </td>
                        <% } %>
                </tr>
                <% } %>
                    <tr>
                        <td colspan="2" style="font-size: x-small; height: 0;">
                            Owner: <%= bingocards[bingocards.length-1].playerName.toUpperCase() %> <br>
                                Card Id: <%= bingocards[bingocards.length-1].cards[0]._id %>
                        </td>
                    </tr>
        </tbody>
    </table>
</body>

</html>